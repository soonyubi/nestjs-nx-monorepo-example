# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build nestjs-monorepo-nx-example`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t nestjs-monorepo-nx-example`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system nestjs-monorepo-nx-example && \
          adduser --system -G nestjs-monorepo-nx-example nestjs-monorepo-nx-example

COPY apps/nestjs-monorepo-nx-example/dist nestjs-monorepo-nx-example/
RUN chown -R nestjs-monorepo-nx-example:nestjs-monorepo-nx-example .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix nestjs-monorepo-nx-example --omit=dev -f install

CMD [ "node", "nestjs-monorepo-nx-example" ]
